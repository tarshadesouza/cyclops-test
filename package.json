{
  "name": "cyclops-test",
  "version": "1.0.0",
  "description": "Test repo for exercising the CyclOps GitHub App pipeline",
  "type": "module",
  "scripts": {
    "lint": "eslint src",
    "test": "node --test"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}

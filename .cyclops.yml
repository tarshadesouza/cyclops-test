# Phase 7 agentic autofix — press "Agent fix (all-in)" and cyclops loops a
# coding agent on this branch until CI is green.
autofix:
  mode: agent
  agent:
    permission: all-in
    maxIterations: 2
    model: claude-sonnet-5
  dryRun: false
confidenceThreshold: 0.85

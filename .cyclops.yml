# Autofix (safe): agent loops on a NEW cyclops/fix/* branch and opens a green PR.
autofix:
  mode: agent
  agent:
    permission: safe
    maxIterations: 2
    model: claude-sonnet-5
confidenceThreshold: 0.85
